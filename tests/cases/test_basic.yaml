name: basic
description: Basic sanity check — bot can do simple arithmetic
skip: false

phases:
  - send: "Hello! What is 2 + 2?"
    expect_reply_contains:
      - "4"
  - send: "and what is 3 + 5?"
    expect_reply_contains:
      - "8"
